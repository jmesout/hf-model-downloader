# Production dependencies for HuggingFace Model Cache Loader
# These are the minimal dependencies required for runtime in production containers

huggingface-hub>=0.26.0,<2.0.0
hf-transfer>=0.1.9,<1.0.0
boto3>=1.35.0,<2.0.0
tqdm>=4.65.0,<5.0.0
